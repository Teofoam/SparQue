# Holds the real NAPCAT_TOKEN and MCP_SECRET
run.bat

# Dump of get_group_list — full group membership, including names and phone numbers
groups.json

# Raw NapCat API dump — same kind of group/member data
raw9.json

__pycache__/
.idea/
